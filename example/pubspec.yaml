name: source_gen_example_usage
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0
  
dependencies:
  json_schema_gen:
    path: ../
  jsontool: ^2.1.0
  collection: ^1.19.1

dev_dependencies:
  build_runner: ^2.0.0
  dart_flutter_team_lints: ^3.1.0
